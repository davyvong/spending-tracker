import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  contentContainer: {
    marginTop: -8,
  },
  chartContainer: {
    marginHorizontal: -24,
  },
  spendingContainer: {
    marginHorizontal: -24,
  },
});

export default styles;
