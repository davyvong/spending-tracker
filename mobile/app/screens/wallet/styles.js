import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  sectionBlock: {
    marginVertical: 12,
  },
  walletCarousel: {
    marginHorizontal: -24,
  },
});

export default styles;
