import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  sectionBlock: {
    marginVertical: 12,
  },
  walletCarousel: {
    marginBottom: 12,
    marginHorizontal: -24,
  },
});

export default styles;
