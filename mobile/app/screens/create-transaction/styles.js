import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  contentContainer: {
    marginTop: -8,
  },
  serverError: {
    marginTop: 8,
  },
});

export default styles;
