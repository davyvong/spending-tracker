import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  card: {
    marginTop: 18,
  },
  cardVisibility: {
    padding: 12,
    position: 'absolute',
    right: 0,
    top: 0,
  },
});

export default styles;
