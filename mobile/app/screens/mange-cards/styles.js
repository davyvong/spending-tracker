import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  card: {
    marginTop: 18,
  },
  cardVisibility: {
    position: 'absolute',
    right: 12,
    top: 12,
  },
});

export default styles;
