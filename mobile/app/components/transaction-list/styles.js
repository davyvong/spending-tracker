import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  contentContainer: {
    paddingTop: 6,
  },
  sectionHeader: {
    marginHorizontal: 24,
    marginVertical: 8,
  },
});

export default styles;
